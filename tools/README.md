# tools/

Intentionally empty stub through P0-P2 (no build targets reference this directory before P3). Activates in P3 with
BOTH the Python modal-extraction pipeline AND the C++ headless triode-table generator. Python is never used
anywhere in P0-P2.
