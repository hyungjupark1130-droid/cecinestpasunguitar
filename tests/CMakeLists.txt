# cnpg_tests / cnpg_bench / cnpg_render / cnpg_calibrate -- headless verification executables.
#
# cnpg_tests (Catch2 v3 + CTest, catch_discover_tests) lands in Task P0.3 alongside the first
# [contract] test. cnpg_bench and cnpg_render land in P1, cnpg_calibrate in P2. Intentionally
# empty for now: this file only needs to exist so add_subdirectory(tests) succeeds.
